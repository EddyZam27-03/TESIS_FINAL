<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@drawable/bg_card_3d"
    app:cardElevation="0dp"
    app:cardCornerRadius="18dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- ✅ NOMBRE DEL MÓDULO - Nivel 1 de la jerarquía -->
        <TextView
            android:id="@+id/tvModuloNombre"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Módulo"
            android:textAppearance="@style/TextAppearance.Headline2"
            android:textColor="?attr/colorPrimary"
            android:textStyle="bold"
            android:paddingBottom="12dp"
            android:paddingStart="4dp" />

        <!-- ✅ RecyclerView de SUBMÓDULOS (Nivel 2: Módulo → Submódulos → Gestos) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvSubmodulos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:visibility="visible"
            tools:listitem="@layout/item_submodulo" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
